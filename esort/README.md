# esort
## Calling Sequence
- `s = esort(p)`
- `[s, ndx] = esort(p)`

## Description
Sort continuous-time poles, `p`, by real part (in decreasing order).

## Parameters
- `p` (vector): Continuous-time poles.

## Dependencies
gsort

## Examples
